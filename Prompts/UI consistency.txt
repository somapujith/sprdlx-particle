Audit this codebase for UI inconsistencies and standardize everything.

SPACING:
  - Find all hardcoded pixel values for margin/padding (margin: 12px, padding: 8px)
  - If using Tailwind: replace with Tailwind spacing classes
  - If using CSS: move to CSS custom properties in styles/theme.js or :root variables
  - Enforce a spacing scale: 4, 8, 12, 16, 24, 32, 48, 64px only

COLORS:
  - Find all hardcoded color hex values outside of theme/variables files
  - Move to CSS custom properties (--color-primary, --color-danger, etc.)
  - Find all places the same color is defined differently (#FF0000 vs red vs rgb(255,0,0))
  - Standardize to one definition

TYPOGRAPHY:
  - Find inconsistent font sizes (mixing 13px, 14px, 15px for body text)
  - Standardize: define a type scale and use only those values
  - Find inconsistent font weights
  - Find headings that aren't using heading tags (h1-h6) — divs styled as headings

COMPONENT REUSE:
  - Find every instance of a custom-styled button that isn't using Button from components/common/
  - Find every inline modal/dialog that should use Modal from components/common/
  - Find every custom input that should use Input from components/common/
  - Replace them all with the common component

REPORT then FIX all violations. Create missing common components if needed.