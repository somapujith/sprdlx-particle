/**
 * Legacy filename — re-exports the GLB particle hero so any stale import/cache
 * still renders the earthquake particle globe (not the old glass spheres).
 */
export { default } from './EarthquakeParticleHero';
