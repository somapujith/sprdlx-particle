Audit this project for accessibility (a11y) violations and fix them all.

SEMANTIC HTML:
  - <div onClick> used instead of <button> — fix: use <button>
  - <div> used for navigation — fix: use <nav>
  - Missing <main>, <header>, <footer> landmarks
  - Headings out of order (h3 before h2, h1 used multiple times per page)
  - Images missing alt attributes
  - Icons without aria-label or aria-hidden

KEYBOARD NAVIGATION:
  - Interactive elements not reachable by Tab
  - Missing focus styles (outline: none with no replacement)
  - Dropdowns/modals that trap focus incorrectly
  - No skip-to-content link at the top of the page

ARIA:
  - Modals missing role="dialog" and aria-modal="true"
  - Form inputs missing associated <label> or aria-label
  - Loading spinners missing role="status" and aria-live
  - Error messages not linked to their input via aria-describedby
  - Toggle buttons missing aria-pressed
  - Expandable sections missing aria-expanded

COLOR:
  - Text with contrast ratio below 4.5:1 (WCAG AA)
  - Interactive elements with contrast below 3:1

FORMS:
  - Required fields not marked with aria-required="true"
  - Error messages not programmatically associated with inputs

Fix every violation. Deliver full updated files.