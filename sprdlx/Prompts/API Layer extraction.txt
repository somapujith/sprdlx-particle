You are refactoring the data layer of this project. Goal: ZERO API calls inside component or section files.

STEP 1 — SCAN AND LIST:
Find every instance of:
  - fetch(), axios.get/post/put/delete(), $.ajax()
  - Any direct HTTP call inside a .jsx or .tsx file
  - Any API URL string (http://, https://, /api/) hardcoded in a component

STEP 2 — EXTRACT TO SERVICES:
For each API call found:
  - Create or update the correct file in src/services/[feature]Service.js
  - Move the call into a named async function with clear params and return type
  - Use the base API client from src/services/api.js (create it if missing)
  - Handle errors inside the service function — throw a clean error, not a raw response

STEP 3 — WRAP IN HOOKS:
For each service function:
  - Create or update a hook in src/hooks/use[Feature].js
  - The hook manages: loading state, error state, data state
  - Expose: { data, isLoading, error, refetch } (or mutation equivalents)
  - Use useCallback/useMemo where appropriate to prevent re-renders

STEP 4 — CONSUME IN COMPONENTS:
  - Replace the old inline fetch with the new hook
  - Components only call hooks — never services directly
  - Remove any useEffect that was only there to fetch data

STEP 5 — ENVIRONMENT VARIABLES:
  - Move every hardcoded base URL to .env as VITE_API_BASE_URL
  - Update .env.example with the new variable (empty value)
  - Never commit actual values

Deliver: full updated service file, hook file, and updated component for each case.